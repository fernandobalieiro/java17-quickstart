package org.acme;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeJava17ResourceIT extends Java17ResourceTest {

    // Execute the same tests but in native mode.
}